"use strict";

if (typeof(global) !== "undefined") {
    global.FStar = global.FStar || {};
}

if (typeof(window) !== "undefined") {
    window.FStar = window.FStar || {};
}

if (typeof(self) !== "undefined") {
    self.FStar = self.FStar || {};
}
